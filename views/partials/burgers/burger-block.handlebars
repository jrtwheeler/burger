<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}<i class="fas fa-utensils"></i>{{else}}<i class="fas fa-hamburger"></i>{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}"><i class="fas fa-trash-alt"></i></button>
</li>
